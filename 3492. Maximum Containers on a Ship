class Solution {
    public int maxContainers(int n, int w, int maxWeight) {
        int totalCells = n*n;
        int reminder = maxWeight/w;
        return Math.min(totalCells ,reminder);    
    }
}
