class Solution {
    public int minMoves(int[] nums) {
        int n= nums.length;
        int sum=0;
        int max=0;
        for(int v:nums)
            {
                max= Math.max(max,v);
                sum+=v;
            }
        return n*max-sum;
    }
}
